import 'package:get/get.dart';

class ProfileController extends GetxController {
  /**
   * final userService = Get.find<UserService>();

// Access user details
String? userName = userService.currentUser?.data.user.name;
String? email = userService.currentUser?.data.user.email;
   */
}
